# FastAPI 核心框架和ASGI服务器 - 使用最新稳定版
# FastAPI core framework and ASGI server - using latest stable version
fastapi==0.115.12
uvicorn==0.34.2
python-multipart==0.0.17

# LangChain 生态系统 - LLM应用开发框架
# LangChain ecosystem - LLM application development framework
langchain==0.3.25
langchain-core==0.3.61
langchain-community==0.3.24
langchain-anthropic==0.3.13
langchain-text-splitters==0.3.8

# LangGraph 工作流编排 - 最新稳定版本
# LangGraph workflow orchestration - latest stable version
langgraph==0.4.5
langgraph-checkpoint==2.0.26

# Anthropic Claude 4 - 最新稳定版本支持Claude 4模型
# Anthropic Claude 4 - latest stable version supporting Claude 4 models
anthropic==0.52.0

# HTTP 客户端和异步编程
# HTTP client and async programming
httpx==0.28.1
requests==2.32.3
aiofiles==24.1.0

# 定时任务调度器
# Task scheduler
apscheduler==3.11.0

# Azure 云服务 SDK - 更新到最新稳定版本
# Azure cloud services SDK - updated to latest stable versions
azure-search-documents==11.5.2
azure-storage-blob==12.21.0
azure-identity==1.19.0
azure-core==1.34.0

# 数据库相关 - 更新版本
# Database related - updated versions
psycopg[binary]==3.2.4
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.21.0

# OAuth 和认证
# OAuth and authentication
authlib==1.3.2
itsdangerous==2.2.0
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.2.1
PyJWT==2.10.1
email-validator==2.2.0

# 数据验证和序列化 - 更新Pydantic到最新稳定版
# Data validation and serialization - updated Pydantic to latest stable
pydantic==2.10.3
pydantic-settings==2.6.1

# 环境变量管理
# Environment variable management
python-dotenv==1.0.1

# 图表和数据处理 - 更新版本(兼容Python 3.9)
# Charts and data processing - updated versions (Python 3.9 compatible)
plotly==6.1.1
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.3.2

# 文档和PDF处理
# Document and PDF processing
pypdf==5.1.0
python-docx==1.1.2

# Redis 缓存 (可选)
# Redis cache (optional)
redis==5.2.1

# 监控和日志
# Monitoring and logging
sentry-sdk[fastapi]==2.20.0

# 开发工具
# Development tools
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.10.0
isort==5.13.2
flake8==7.1.1

# 机器学习和向量化
# Machine learning and vectorization
tiktoken==0.8.0 